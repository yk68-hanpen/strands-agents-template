version: '3.8'

services:
  simple-agent:
    build: .
    container_name: simple-agent
    env_file:
      - .env
    volumes:
      - ./src:/app/src
    environment:
      - PYTHONUNBUFFERED=1
    command: python src/main.py "こんにちは！今何時ですか？また、10 * 25 を計算してください。"
