name: Scheduled Agent

on:
  schedule:
    # 毎週月曜日の9:00 (UTC) に実行
    - cron: '0 9 * * 1'
  workflow_dispatch:
    inputs:
      prompt:
        description: 'エージェントへのプロンプト'
        required: false
        default: 'こんにちは！今何時ですか？'

jobs:
  run-scheduled-agent:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
      
      - name: Run agent
        env:
          AWS_REGION: ${{ secrets.AWS_REGION }}
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          MODEL_ID: ${{ secrets.MODEL_ID }}
        run: |
          PROMPT="${{ github.event.inputs.prompt || 'こんにちは！今何時ですか？' }}"
          python src/main.py "$PROMPT"
      
      - name: Create issue with result
        uses: actions/github-script@v7
        with:
          script: |
            const date = new Date().toISOString().split('T')[0];
            github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `🤖 定期実行結果: ${date}`,
              body: 'エージェントが正常に実行されました。詳細はActionsログを確認してください。',
              labels: ['automated', 'scheduled']
            });
