name: PR Agent

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  review-pr:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.14'
      
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
      
      - name: Analyze PR changes
        id: analyze
        run: |
          # PRの変更内容を分析（カスタマイズ可能）
          echo "files_changed=$(git diff --name-only origin/${{ github.base_ref }}...HEAD | wc -l)" >> $GITHUB_OUTPUT
      
      - name: Comment on PR
        uses: actions/github-script@v7
        with:
          script: |
            const filesChanged = ${{ steps.analyze.outputs.files_changed }};
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `🤖 PR分析結果\n\n変更されたファイル数: ${filesChanged}\n\nこのワークフローは汎用的なテンプレートです。プロジェクトに応じてカスタマイズしてください。`
            })
